<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Watchlist</title>
</head>
<body>
    <h1>Movie Watchlist</h1>

    <form action="/add" method="POST">
        <input type="text" name="title" placeholder="Movie Title" required>
        <input type="text" name="genre" placeholder="Genre">
        <input type="number" name="year" placeholder="Year" min="1900">
        <input type="number" name="rating" placeholder="Rating" min="0" max="10" step="0.1">
        <button type="submit">Add Movie</button>
    </form>

    <form method="GET" action="/">
        <label for="genre">Filter by Genre:</label>
        <select name="genre" id="genre">
            <option value="All" {{#equal selectedGenre "All"}}selected{{/equal}}>All</option>
            <option value="Action" {{#equal selectedGenre "Action"}}selected{{/equal}}>Action</option>
            <option value="Comedy" {{#equal selectedGenre "Comedy"}}selected{{/equal}}>Comedy</option>
            <option value="Drama" {{#equal selectedGenre "Drama"}}selected{{/equal}}>Drama</option>
        </select>

        <label for="sort">Sort by:</label>
        <select name="sort">
            <option value="">None</option>
            <option value="rating">Rating</option>
        </select>

        <button type="submit">Apply</button>
        <a href="/" style="background-color: red; color: white; padding: 5px; text-decoration: none;">Clear</a>
    </form>

    <ul>
        {{#movies}}
        <li style="background-color: {{#watched}}lightgrey{{/watched}}; font-weight: {{#watched}}bold{{/watched}};">
            {{title}} ({{year}}) - {{genre}} - Rating: {{rating}}
            <form action="/update/{{id}}" method="POST">
                <button type="submit">Toggle Watched</button>
            </form>
            <form action="/delete/{{id}}" method="POST">
                <button type="submit">Delete</button>
            </form>
        </li>
        {{/movies}}
    </ul>
</body>
</html>
